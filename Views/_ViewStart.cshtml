@{
    // this view is called before any other vies below this folder
    // defining the layout here, all sub views will use the same layout unless overwritten in hte view itself
    Layout = "~/Views/Shared/_Layout.cshtml";
}
